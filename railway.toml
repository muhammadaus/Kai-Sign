[build]
builder = "NIXPACKS"
watchPatterns = ["*.py", "requirements.txt"]

[deploy]
startCommand = "uvicorn api.index:app --host 0.0.0.0 --port ${PORT:-8000}"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10 