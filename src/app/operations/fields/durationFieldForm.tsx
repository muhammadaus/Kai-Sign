import { FormDescription } from "~/components/ui/form";

const DurationFieldForm = () => {
  return (
    <div>
      <FormDescription>
        Display int as a duration interpreted in seconds and represented as a
        duration HH:MM:ss
      </FormDescription>
    </div>
  );
};

export default DurationFieldForm;
